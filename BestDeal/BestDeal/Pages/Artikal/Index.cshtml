@page
@model BestDeal.Controllers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Artikal[0].CijenaArtikla)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Artikal) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CijenaArtikla)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdArtikla">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdArtikla">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdArtikla">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

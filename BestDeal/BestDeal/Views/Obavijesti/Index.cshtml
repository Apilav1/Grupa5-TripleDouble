@model IEnumerable<BestDeal.Models.Obavijest>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TekstObavijesti)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TekstObavijesti)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdObavijesti">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdObavijesti">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdObavijesti">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
